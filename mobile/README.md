# Solarsearch mobile (Capacitor iOS shells)

Two native iOS shells that wrap the existing web apps so they can go to TestFlight /
the App Store, with native camera, geolocation and push. Each loads its hosted
**launcher page** over the air (`server.url`), so day‑to‑day changes ship by deploying
the website — no App Store resubmission — while the native bridge (camera/geo/push)
still works against the remote content.

> The `ios/` Xcode project is generated by `cap add ios` and **must be run on a Mac
> with Xcode**. It is not committed here.

## Two apps (one Capacitor project each)
| App | Folder | Bundle ID | Loads | Signed-in routing |
|-----|--------|-----------|-------|-------------------|
| **Field** (Sales Technician) | `mobile/field` | `au.com.solarsearch.field` | `/app-field.html` | `sales_rep` → `/tech.html` |
| **Installer** | `mobile/installer` | `au.com.solarsearch.installer` | `/app-installer.html` | `installer` → `/installer.html` |

Live host: **https://www.solarsearch.com.au** (set in each `capacitor.config.ts`).

> **Confirm the bundle IDs** match the App IDs in your Apple Developer account before
> `cap add ios`. If you prefer a different namespace, change `appId` in the config first.

## Access + Apple requirements already wired in the web app
- **Access is by application** — the launcher "Apply" button opens `/apply.html?role=…`,
  which creates the account if needed, records the **required T&C acknowledgement**, and
  writes `access_applications`. Admin grants in HQ → Vetting → "App access applications"
  (grant is blocked until T&Cs are accepted). On grant the role record is auto‑provisioned.
- **In‑app account deletion** (Apple 5.1.1(v)) — a "Delete my account" action exists in
  `tech.html`, `install.html` and `apply.html` (`request_account_deletion`).
- **Privacy policy** — `app-privacy.html`, live at `/app-privacy.html` (separate from the
  consumer `privacy.html`). Entity/ABN/region filled; confirm address/phone before submitting.
- **App Store metadata + privacy‑label answers** — `mobile/APP_STORE_METADATA.md`
  (copy‑paste into App Store Connect; includes the single‑role demo‑login note so review
  isn't blocked at the access gate).

## Build steps (on a Mac) — per app
```bash
cd mobile/field        # or: cd mobile/installer
npm install
npx cap add ios        # generates ios/ (Mac only)
npx cap sync           # copies www + native plugins into the project
npx cap open ios       # Xcode → set Team + bundle id → app icon → Product > Archive
```
Then in Xcode: select your Team (the Apple org you use for inspector/voya), confirm the
bundle id, add the app icon, and **Archive → distribute to TestFlight**.

Because the app loads `server.url`, iterating on the web app needs **no rebuild** — just
deploy the site. Rebuild only when you change native config, plugins, or the icon.

## Required — iOS permission strings (Info.plist)
`cap add ios` creates `ios/App/App/Info.plist`. Add these (or the app is rejected / crashes
on first camera/GPS use) — the text is shown in the OS permission prompt:
```xml
<key>NSCameraUsageDescription</key>
<string>Take site photos for your solar assessment.</string>
<key>NSLocationWhenInUseUsageDescription</key>
<string>Tag site photos and confirm on-site attendance.</string>
<key>NSPhotoLibraryUsageDescription</key>
<string>Attach drone or existing photos to a job.</string>
<key>NSPhotoLibraryAddUsageDescription</key>
<string>Save captured job photos.</string>
```

## App Store Connect checklist
- [ ] Apple Developer **Organization** account (you have one — inspector/voya).
- [ ] Info.plist usage strings (above).
- [ ] **Privacy Policy URL** live at `/app-privacy.html`.
- [ ] **In‑app account deletion** — built (`request_account_deletion`).
- [ ] **Sign in with Apple** — only required if you add Google/Facebook login; email/password alone doesn't.
- [ ] **Single‑role demo login** for reviewers (see APP_STORE_METADATA.md — not an admin account).
- [ ] App icon (1024×1024) + launch screen.
